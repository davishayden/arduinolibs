Contains custom libraries for use with the arduino platform

To use, drag and drop relevant library folder into the libraries directory where
the arduino IDE is installed.

Currently contains:
1) WiFi Twitter library: Allows one to tweet wirelessly using the new Arduino 
WiFi shield.